"use client";

import { LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer } from "recharts";

export default function EnrollmentChart() {
  return (
    <div>
      <h1>Enrollment Chart</h1>
    </div>
  );
}
